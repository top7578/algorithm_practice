import sys
input = lambda : sys.stdin.readline().rstrip()

def check(i):
  if i == -1:
    print('I hate UCPC')
    exit()

ucpc = ['U', 'C', 'P', 'C']

s = input()

index = -1
for x in ucpc:
  index = s.find(x, index+1)
  check(index)

print('I love UCPC')
